module.exports = (function(){

	var global = require('../exts/toolstack'),
	http = require('http'),
	url = require('url');
	global.ExtInit(global);	
	require('../src/core').Core(global);
	


	return {
		http: http,
		url: url,
		core: global.Core,
		toolstack: global.ToolStack,
		specs: require("../builds/core.spec")
	};

})();